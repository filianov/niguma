# 🚀 Запуск 15minyoga.com — дорожная карта

Пошаговый план вывода проекта на боевой домен **15minyoga.com** и полная карта
всех параметров, которые нужно прописать, чтобы не осталось хвостов.

Документ построен на **аудите текущего кода** (июль 2026), а не на общих словах:
каждая строка ниже проверена по файлам проекта.

---

## 0. Результат аудита: что готово, чего не хватает

### ✅ Готово и работает
| Что | Где |
|---|---|
| Лендинг, 4 языка (RU/EN/DE/UK) | `landing/` |
| Фирменный стиль, логотипы, фавиконы, OG-обложка | `landing/assets/brand/`, `brandbook/` |
| Фотоконтент | `landing/assets/photos/` (6 файлов) |
| Telegram-агент: поддержка, воронка, напоминания, оплата, геймификация | `bot/` |
| Конфиг заголовков и кэша для хостинга | `landing/vercel.json` |
| Политика конфиденциальности | `landing/legal/privacy.html` |
| Манифест, robots.txt, sitemap.xml | `landing/` |

### ⚠️ Хвосты, которые надо закрыть до запуска
| № | Хвост | Критичность | Кто закрывает |
|---|---|---|---|
| 1 | ~~Коммиты не отправлены~~ · сайт живёт на 15minyoga.com | ✅ закрыто | — |
| 2 | ~~Impressum отсутствует~~ · опубликован на 4 языках (Olha Filianova) | ✅ закрыто | — |
| 3 | `REPLACE_ME` — 5 шт. в `config/project.config.json` (PayPal, IBAN, BIC, банк, Monobank) | 🔴 блокер оплаты | вы дадите реквизиты |
| 4 | `REPLACE_ME` — 5 шт. в `bot/.env.example` (Claude-ключ, 2× Zoom, Monobank, PayPal) | 🔴 блокер бота | вы дадите ключи |
| 5 | ~~`meta.description` устарел~~ · обновлён на 4 языках | ✅ закрыто | — |
| 6 | ~~Форма никуда не отправляет~~ · `/api/lead` → Telegram + почта | ✅ код готов | вы добавите ключи в Vercel |
| 7 | Платёжные ссылки пустые: `pay.paypal`, `pay.stripe` | 🟠 | вы дадите ссылки |
| 8 | ~~Нет `hreflang`~~ · добавлены 5 тегов | ✅ закрыто | — |
| 9 | Нет аналитики (Plausible/GA) | 🟡 | по вашему решению |
| 10 | Нет оферты / условий участия | 🟡 | шаблон от меня + юрист |
| 11 | Нет PDF `assets/docs/15minyoga-method.pdf` (блок скрыт, ждёт файл) | 🟢 не блокер | вы пришлёте |

---

## 1. Карта связей — что с чем соединяется

```
                        ┌──────────────────────┐
   Регистратор домена ──▶│   15minyoga.com      │
   (DNS: A / CNAME)      └──────────┬───────────┘
                                    │
                 ┌──────────────────┼───────────────────┐
                 ▼                  ▼                   ▼
        ┌────────────────┐   ┌─────────────┐    ┌──────────────┐
        │ Vercel         │   │ Почта       │    │ MX-записи    │
        │ (лендинг)      │   │ hello@…     │    │ SPF/DKIM/    │
        │ из GitHub      │   │             │    │ DMARC        │
        └───────┬────────┘   └──────┬──────┘    └──────────────┘
                │                   │
   кнопки сайта │                   │ письма/анонсы
                ▼                   ▼
        ┌────────────────────────────────────┐
        │  Telegram-бот  @yoga15min_bot      │◀── Railway/Fly (хостинг процесса)
        │  • воронка • оплата • напоминания  │◀── ANTHROPIC_API_KEY (Claude)
        └───────┬───────────────┬────────────┘
                │               │
      выдаёт    ▼               ▼  ведёт участников
        ┌──────────────┐  ┌─────────────────────┐
        │ Реквизиты    │  │ Приватный канал     │
        │ PayPal/IBAN/ │  │ @yoga15min          │
        │ Monobank     │  │ + Zoom-ссылки       │
        └──────────────┘  └─────────────────────┘
                ▲
                │ эскалация сложных вопросов
        ┌───────────────┐
        │ Instagram DM  │ @15minyoga
        └───────────────┘
```

**Главный принцип связности:** имена аккаунтов уже «зашиты» в код. Создавайте их
**ровно такими** — иначе ссылки на сайте будут вести в пустоту:

| Сервис | Имя, которое ждёт код | Почему именно так |
|---|---|---|
| Telegram-бот | **@yoga15min_bot** | Telegram запрещает имена, начинающиеся с цифры |
| Telegram-канал | **@yoga15min** | то же ограничение |
| Instagram | **@15minyoga** | — |
| Facebook | **facebook.com/15minyoga** | — |
| Почта | **15minyoga.com@gmail.com** | — |

> Если какое-то имя окажется занято — скажите, я поменяю его во всех файлах разом
> (оно встречается в 3 местах: `landing/js/config.js`, `config/project.config.json`, `bot/`).

---

## 2. ШАГ 1 — Отправить код на GitHub *(5 минут, делаете вы)*

Сейчас 2 коммита лежат только локально.

1. Откройте **GitHub Desktop**.
2. Нажмите **Push origin**.
3. Убедитесь, что на github.com/filianov/niguma появились последние изменения.

Без этого шага Vercel развернёт старую версию.

---

## 3. ШАГ 2 — Хостинг + домен *(20 минут)*

### 3.1 Развернуть лендинг на Vercel
1. Зайти на [vercel.com](https://vercel.com) → **Sign up with GitHub**.
2. **Add New… → Project** → выбрать репозиторий `niguma` → **Import**.
3. **Root Directory** → нажать **Edit** → выбрать папку **`landing`**. ← критично!
4. Framework Preset: **Other**. Build Command и Output — оставить пустыми (это статика).
5. **Deploy**. Через ~30 сек получите адрес вида `niguma.vercel.app` — проверьте, что сайт открылся.

### 3.2 Подключить домен `15minyoga.com` (регистратор — **imena.ua**)

1. В проекте Vercel → **Settings → Domains** → **Add Domain** → ввести `15minyoga.com`.
2. Vercel предложит добавить и `www.15minyoga.com` — согласиться (сделает редирект на основной).
3. Vercel покажет **две записи, которые надо прописать**. Значения берите **из своей панели
   Vercel**, не из интернета:
   - для апекса `15minyoga.com` — запись **A** с IP, который показывает Vercel
     (обычно `76.76.21.21`, но проверьте на экране);
   - для `www` — запись **CNAME**. ⚠️ Vercel выдаёт **уникальный адрес под каждый проект**,
     вида `d1d4fc829fe7bc7c.vercel-dns-017.com` — старый общий `cname.vercel-dns.com`
     больше не универсален. Копируйте ровно то, что показано в панели.

4. **Прописать записи в imena.ua:**
   1. Войти на [cp.imena.ua](https://cp.imena.ua) (или my.imena.ua).
   2. Раздел **Домени / Домены** → выбрать `15minyoga.com`.
   3. Вкладка **Налаштування домену / Настройка домена → DNS-записи**.
   4. Убедиться, что домен использует **DNS-серверы imena.ua** (если делегирован куда-то
      ещё — записи надо прописывать там).
   5. Добавить:

      | Тип | Имя (Host) | Значение |
      |---|---|---|
      | **A** | `@` (или пусто / сам домен) | IP из панели Vercel |
      | **CNAME** | `www` | уникальный адрес из панели Vercel |

   6. Если в панели уже есть **A-запись для `@`** от парковочной страницы регистратора —
      **удалить её**, иначе домен будет вести на заглушку.
   7. Нажать **Зберегти / Сохранить**.

5. Подождать обновление DNS: у imena.ua обычно **15–60 минут**, регламент — до 24 часов.
   Проверить, что записи применились: `dig 15minyoga.com +short` (должен вернуть IP Vercel).
6. В панели Vercel статус домена сменится на **Valid Configuration**, сертификат выпустится сам.
   Проверить: **https://15minyoga.com** открывается с замком.

> 💡 **На будущее.** Дальше понадобится много DNS-записей для почты (MX + SPF + DKIM + DMARC).
> Панель imena.ua для этого рабочая, но небогатая. Когда дойдём до почты, можно перевести
> обслуживание DNS на **Cloudflare** (домен остаётся у imena.ua, меняются только NS-серверы):
> удобнее интерфейс, изменения применяются за секунды, бесплатный CDN. Не обязательно —
> просто удобнее. Записи Vercel в Cloudflare ставятся в режиме **DNS only** (серое облако).

---

## 4. ШАГ 3 — Почта на домене *(30 минут)*

Нужна для `15minyoga.com@gmail.com` — она уже прописана на сайте и в боте.

1. Выбрать провайдера: **Zoho Mail** (есть бесплатный тариф на 1 домен) или
   **Google Workspace** (~6 €/мес) или почта регистратора.
2. Создать ящик `15minyoga.com@gmail.com`.
3. Прописать в DNS выданные провайдером **MX**-записи.
4. **Обязательно** настроить три записи — без них письма от бота уйдут в спам:
   - **SPF** (TXT): разрешает вашему провайдеру слать письма от домена
   - **DKIM** (TXT): подпись писем
   - **DMARC** (TXT): политика, начать с `v=DMARC1; p=none; rua=mailto:15minyoga.com@gmail.com`
5. Проверить настройку на [mail-tester.com](https://www.mail-tester.com) — цель 9–10/10.

---

## 5. ШАГ 4 — Telegram *(20 минут)*

> 👉 Подробная пошаговая версия этого шага: [BOT-SETUP.md](BOT-SETUP.md)

1. **Бот:** написать [@BotFather](https://t.me/BotFather) → `/newbot` → имя **`yoga15min_bot`**
   → получить `BOT_TOKEN` (длинная строка — это пароль, никому не показывать).
2. Там же: `/setuserpic` — загрузить `brandbook/logo/png/app-icon-512.png`;
   `/setdescription` и `/setabouttext` — короткое описание проекта.
3. **Свой Telegram ID:** написать [@userinfobot](https://t.me/userinfobot) → он ответит числом → это `ADMIN_IDS`.
4. **Приватный канал участников:** создать канал → назначить бота администратором →
   узнать его ID (переслать любое сообщение канала в [@userinfobot](https://t.me/userinfobot)) → это `MEMBERS_CHANNEL_ID`.
5. **Публичный канал** (по желанию): `@yoga15min` — на него ведёт ссылка в подвале сайта.

---

## 6. ШАГ 5 — Соцсети *(15 минут)*

1. Instagram: занять **@15minyoga**, аватар — `app-icon-512.png`, в шапку — ссылку `15minyoga.com`.
2. Facebook: страница **facebook.com/15minyoga**.
3. YouTube: баннер `brandbook/logo/social/banner-youtube-photo-2560x1440.png`, аватар — тот же app-icon.

---

## 7. ШАГ 6 — Платежи *(1–2 часа, см. [PAYMENTS.md](PAYMENTS.md))*

Минимум для старта — ручные реквизиты, их выдаёт бот:

| Параметр | Где взять | Куда вписать |
|---|---|---|
| `PAYPAL_ME` | paypal.me — создать ссылку | `bot/.env` + `project.config.json` |
| `EUR_IBAN`, `EUR_BIC`, `EUR_BENEFICIARY`, `EUR_BANK` | ваш немецкий банк | `bot/.env` + `project.config.json` |
| `MONOBANK_JAR` | приложение Monobank → «Банка» → поделиться | `bot/.env` + `project.config.json` |

Автоматический checkout (можно позже): `PAY_LINK_M1/M6/M12` — ссылки PayPal-подписок
или Stripe Payment Links. **Рекомендация из аудита платежей: Paddle (Merchant of Record)** —
он берёт НДС всех стран на себя, вам не нужно регистрировать OSS.

---

## 8. ШАГ 7 — Zoom *(10 минут)*

1. Создать **постоянную** комнату (Personal Meeting Room или recurring meeting).
2. Включить автозапись в облако — записи нужны участникам.
3. Ссылку буднего занятия → `STREAM_LINK`, субботнего → `STREAM_LINK_WEEKLY`.

---

## 9. ШАГ 8 — Запустить бота *(30 минут)*

1. Ключ Claude: [console.anthropic.com](https://console.anthropic.com) → API Keys → создать → `ANTHROPIC_API_KEY`.
2. [railway.app](https://railway.app) → **New Project → Deploy from GitHub repo** → `niguma`.
3. Settings: **Root Directory = `bot`**, **Start Command = `python main.py`**.
4. **Variables** — перенести все переменные из `bot/.env.example` с реальными значениями.
5. Deploy → в логах должно появиться `15minYoga bot polling…` и список cron-задач.
6. Проверить в Telegram: `/start` → квалификатор → `/pay` → приходят ли реквизиты.

---

## 10. ШАГ 9 — Юридические страницы *(критично для Германии)*

| Страница | Статус | Что нужно |
|---|---|---|
| Политика конфиденциальности | ✅ черновик есть | проверка юристом |
| **Impressum** | ❌ **нет** | обязателен по §5 DDG: имя, адрес, e-mail, форма деятельности, при наличии — УНН |
| Оферта / условия участия | ❌ нет | правила оплаты, возврата, доступа к записям |
| Право на отказ (Widerruf) | ❌ нет | у потребителей в ЕС 14 дней; для цифровых услуг нужно явное согласие на немедленный доступ |

> ⚠️ В Германии отсутствие Impressum — самая частая причина «Abmahnung» (платного
> предупреждения от конкурентов или юрфирм). Это первое, что стоит закрыть.
> Я подготовлю шаблоны — от вас нужны юридические данные (имя, адрес регистрации).

---

## 11. ШАГ 10 — SEO и аналитика *(после запуска)*

1. Добавить `hreflang` для 4 языков — **сделаю я**.
2. Обновить `meta.description` под новое позиционирование — **сделаю я**.
3. [Google Search Console](https://search.google.com/search-console) → подтвердить домен → отправить `sitemap.xml`.
4. Аналитика: **Plausible** (без кук, не требует баннера согласия — важно для GDPR)
   или GA4 (тогда нужен cookie-баннер).

---

## 11-bis. Форма подписки — переменные в Vercel

Форма на сайте отправляет заявку в `/api/lead`. Функция уже развёрнута; чтобы она
начала доставлять, добавьте переменные в **Vercel → Settings → Environment Variables**
(область **Production**), затем **Deployments → Redeploy**.

| Переменная | Значение | Обязательна |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | токен от @BotFather | для уведомлений в Telegram |
| `TELEGRAM_ADMIN_ID` | ваш ID от @userinfobot | для уведомлений в Telegram |
| `RESEND_API_KEY` | ключ с resend.com (бесплатно 3000 писем/мес) | для дубля на почту |
| `LEAD_EMAIL_TO` | `15minyoga.com@gmail.com` | по умолчанию уже этот адрес |
| `LEAD_EMAIL_FROM` | `15minYoga <onboarding@resend.dev>` | до верификации домена в Resend |

Каналы независимы: если задан только Telegram — заявки идут в Telegram, если только
Resend — на почту. Пока не задано ничего, заявка пишется в лог Vercel и посетитель
всё равно видит благодарность (терять заявку из-за наших настроек нельзя).

Проверка после настройки: отправьте форму на сайте со своим адресом — уведомление
должно прийти в Telegram и на `15minyoga.com@gmail.com`. Ответ на письмо уходит
напрямую человеку: в письме проставлен `reply_to`.

---

## 12. Полная таблица параметров — «что, откуда, куда»

Держите её под рукой: заполнив всё, вы гарантированно не оставите хвостов.

| # | Параметр | Откуда взять | Куда вписать |
|---|---|---|---|
| 1 | `BOT_TOKEN` | @BotFather | Railway Variables |
| 2 | `ADMIN_IDS` | @userinfobot | Railway Variables |
| 3 | `MEMBERS_CHANNEL_ID` | ID приватного канала | Railway Variables |
| 4 | `ANTHROPIC_API_KEY` | console.anthropic.com | Railway Variables |
| 5 | `STREAM_LINK` | Zoom, будни | Railway Variables |
| 6 | `STREAM_LINK_WEEKLY` | Zoom, суббота | Railway Variables |
| 7 | `EUR_IBAN` / `EUR_BIC` / `EUR_BENEFICIARY` / `EUR_BANK` | немецкий банк | Railway + `project.config.json` |
| 8 | `MONOBANK_JAR` | Monobank «Банка» | Railway + `project.config.json` |
| 9 | `PAYPAL_ME` | paypal.me | Railway + `project.config.json` + `landing/js/config.js` |
| 10 | `PAY_LINK_M1/M6/M12` | PayPal-подписки / Stripe / Paddle | Railway + `landing/js/config.js` |
| 11 | `EMAIL_FROM` | `15minyoga.com@gmail.com` | Railway Variables |
| 12 | `RESEND_API_KEY` | resend.com (для писем) | Railway Variables |
| 13 | DNS: A / CNAME | Vercel | панель регистратора |
| 14 | DNS: MX / SPF / DKIM / DMARC | почтовый провайдер | панель регистратора |
| 15 | Instagram / Facebook / Telegram-канал | ваши аккаунты | `landing/js/config.js` + `project.config.json` |
| 16 | Юридические данные для Impressum | ваши реквизиты предпринимателя | `landing/legal/impressum.html` |
| 17 | PDF о методе | ваш файл | `landing/assets/docs/15minyoga-method.pdf` |

---

## 13. Финальная проверка «без хвостов»

Пройтись по списку **после** выполнения всех шагов:

**Сайт**
- [ ] `https://15minyoga.com` открывается с валидным SSL
- [ ] `www.15minyoga.com` редиректит на основной домен
- [ ] Переключаются все 4 языка, тексты корректны
- [ ] Кнопка «Начать в Telegram» открывает **@yoga15min_bot**
- [ ] Кнопка Instagram открывает **@15minyoga**
- [ ] Ссылки в подвале (Facebook, Telegram, почта, политика) — рабочие
- [ ] Превью ссылки в мессенджере показывает OG-обложку (проверить на [opengraph.xyz](https://www.opengraph.xyz))
- [ ] Фавикон виден во вкладке
- [ ] Мобильная версия: логотип, кнопки, фото — на месте
- [ ] Форма e-mail отправляет лид (не только в localStorage)

**Бот**
- [ ] `/start` создаёт лида, задаёт вопросы
- [ ] `/pay` выдаёт реальные реквизиты (не `REPLACE_ME`)
- [ ] Приём чека → уведомление вам → `/confirm` активирует участника
- [ ] Напоминания приходят по расписанию (проверить в назначенное время)
- [ ] Сложный вопрос → эскалация приходит вам в Telegram
- [ ] `/stats` показывает данные

**Почта**
- [ ] Письмо на `15minyoga.com@gmail.com` доходит
- [ ] Тест на mail-tester.com ≥ 9/10

**Юридическое**
- [ ] Impressum опубликован и доступен из подвала
- [ ] Политика конфиденциальности актуальна
- [ ] Оферта опубликована

**Безопасность**
- [ ] `.env` НЕ в репозитории (проверено: он в `.gitignore`)
- [ ] Токены только в переменных окружения хостинга
- [ ] Бэкап базы бота настроен

---

## 14. Порядок действий — что за чем

Зависимости важны: некоторые шаги нельзя поменять местами.

```
1. Push на GitHub          ──┐
2. Vercel + домен          ──┼──▶ сайт живой
3. Почта (MX/SPF/DKIM)     ──┘
                              │
4. Telegram (бот, каналы)  ───┼──▶ 8. Бот на Railway ──▶ приём заявок
5. Соцсети                 ───┤
6. Платежи (реквизиты)     ───┤
7. Zoom                    ───┘
                              │
9. Юр. страницы            ───┴──▶ 10. SEO/аналитика ──▶ ЗАПУСК
```

**Минимальный путь до «можно продавать»:** шаги 1 → 2 → 4 → 6 → 7 → 8.
Почту, SEO и аналитику можно докрутить в первую неделю; Impressum — до первой рекламы.

---

## 15. Что я могу сделать прямо сейчас, без ваших доступов

- [ ] Шаблон **Impressum** + оферты (с полями под ваши данные)
- [ ] Исправить устаревший `meta.description` на всех 4 языках
- [ ] Добавить `hreflang` для мультиязычного SEO
- [ ] Подключить сбор e-mail с формы (serverless-функция на Vercel → уведомление вам)
- [ ] Подключить Plausible (одна строка, GDPR-friendly)
- [ ] Заменить имена аккаунтов, если какое-то окажется занято

Скажите, с чего начинаем — и по каждому шагу я буду вести вас за руку.
